import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  pickerInput: {
    display: 'flex',
    width: '100%',
    height: 55,
    backgroundColor: 'white',
    borderRadius: 20,
    paddingBottom: 20,
  },
});
