import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    width: '100%',
    height: 55,
    backgroundColor: 'white',
    borderRadius: 20,
    paddingLeft: 10
  },
});
